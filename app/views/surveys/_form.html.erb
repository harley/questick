<%= simple_form_for survey do |f| %>
  <%= f.input :title %>
  <%= f.input :passcode %>

  <div class="form-group">
    <%= f.submit 'Save', class: 'btn btn-default', data: {disable_with: 'Please wait...'} %>
  </div>
<% end %>
